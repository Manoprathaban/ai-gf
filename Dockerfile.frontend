# Step 1: Use the official Nginx image
FROM nginx:alpine

# Step 2: Copy the static files (HTML, CSS, JS) into the Nginx default directory
COPY ./frontend /usr/share/nginx/html

# Step 3: Expose port 80 to allow access to the frontend
EXPOSE 80
